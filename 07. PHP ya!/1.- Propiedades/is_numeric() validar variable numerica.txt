Validar si se ingreso un numero. 

is_numeric(variable). Retorna true si la variable almacena un número, falso en caso contrario, de esta forma podemos validar si se han cargado valores numéricos en cada control "text"